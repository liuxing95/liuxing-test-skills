{
  "name": "skill-publish-test",
  "version": "0.0.1",
  "description": "AI skills plugin mirrored from GitLab to GitHub.",
  "author": {
    "name": "liuxing95"
  },
  "license": "MIT",
  "packageManager": "pnpm@10.33.0",
  "keywords": [
    "ai",
    "skills",
    "plugin",
    "gitlab",
    "github-sync"
  ],
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "validate": "node scripts/assert-version-sync.mjs && node --test tests/*.test.mjs",
    "build": "node scripts/build-plugin.mjs",
    "prepare:github-sync": "./scripts/prepare-github-sync.sh",
    "assert:version": "node scripts/assert-version-sync.mjs",
    "sync:version": "node scripts/sync-version.mjs",
    "assert:github-sync": "node scripts/assert-github-sync-boundary.mjs",
    "changeset": "changeset",
    "version-package": "changeset version && node scripts/assert-version-sync.mjs",
    "release:publish": "node scripts/assert-version-sync.mjs && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.7"
  },
  "files": [
    ".claude-plugin",
    "skills",
    "README.md",
    "LICENSE"
  ]
}
